import { ADMIN_BASE_PATH } from ".";

export const ADMIN_ADMINISTRATION_PATH = {
      base: {
            basic: "administration",
            full: `/${ADMIN_BASE_PATH}/administration`,
      },

      user: {
            basic: "user",
            full: `/${ADMIN_BASE_PATH}/administration/user`,
      },
}